services:
  - type: web
    name: bubble-sort-game
    # CHANGE: Use 'docker' instead of 'java'
    env: docker
    # We remove buildCommand/startCommand because the Dockerfile handles them
    plan: free
    envVars:
      - key: JAVA_VERSION
        value: 21